<!DOCTYPE html>
<html>
	<head>
		<title></title>
	</head>

	<style type="text/css">
	

		#mainImage {
			height: 400px;
		}

		#cityImg {
			max-width: 100%;
			max-height: 100%;
		}

	</style>

	<body>
		<div id="mainImage">
			<img id="cityImg" src="<%= @place.picture %>">
		</div>
		<%= @place.name %>

		<% @posts.each do |post| %>

			<h1><%= post.title %></h1>
			<p><%= post.body %></p>

		<% end %>

	</body>
</html>

